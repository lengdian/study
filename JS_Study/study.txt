js study

注意：
	JS是区分大小写的
	当用户退出页面时（页面关闭、页面刷新等），触发onUnload事件
	
	
	
js对象：JavaScript 中的所有事物都是对象，如:字符串、数值、数组、函数等，每个对象带有属性和方法。
	window对象：是BOM（浏览器对象模型）的核心，window对象指当前的浏览器窗口。
	@location对象:用于获取或设置窗体的URL，并且可以用于解析URL。
	@其他浏览器对象
	
	
	
文档对象模型DOM（Document Object Model）:定义访问和处理HTML文档的标准方法。DOM将HTML文档呈现为带有元素、属性和文本的树结构（节点树）。
HTML文档可以说是由节点构成的集合，三种常见的DOM节点:
	1. 元素节点：<html>、<body>、<p>等都是元素节点，即标签。
	2. 文本节点：向用户展示的内容，如<li>...</li>中的JavaScript、DOM、CSS等文本。
	3. 属性节点：元素属性，如<a>标签的链接属性href="http://www.imooc.com"。

@DOM在前端所处位置（在‘DOM探索之基础详解篇’）
@DOM级别
@常用节点类型  -文档节点表示为document对象  -留意下文档片段节点，它并不在dom树中出现，只是作为一个部分文档占位符
@节点的nodeName与nodeVlaue
@domReady	->domReady.html
@浏览器渲染引擎的基本渲染流程  
@webkit主要渲染流程
@元素节点判定、判定节点是html节点还是xml节点、判断节点的包含关系
@DOM节点继承层次！！
@html嵌套规则



js event（在‘DOM事件探秘’）
@事件流--描述的是从页面中接受事件的顺序
	1、事件冒泡流
	2、事件捕获流
	
@事件处理程序
	1、html事件处理程序
	2、DOM0级事件处理程序
	3、DOM2级事件处理程序
	
@事件对象
	
一般通过‘能力检测’区分IE与其他浏览器，并分别使用不同的方式实现相同的功能。





！！！立即调用的函数表达式
==与===
js对象与对象原型
mvc与mvvm
.与中括号的互用
事件句柄
	